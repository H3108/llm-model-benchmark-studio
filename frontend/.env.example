# Backend API base URL
VITE_API_URL=http://localhost:8000
# NOTE: the admin token is entered manually in the UI and kept in the browser's
# localStorage only — it is deliberately never injected via VITE_* vars, so it
# never gets compiled into the frontend bundle.
